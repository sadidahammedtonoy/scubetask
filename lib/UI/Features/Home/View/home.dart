import 'package:flutter/material.dart';

class scube_home extends StatelessWidget {
  const scube_home({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
