import 'package:flutter/material.dart';

class scube_login extends StatelessWidget {
  const scube_login({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
